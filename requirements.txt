# MCP Detector — Python dependencies
# IEEE Access paper: "Lightweight Explainable ML for MCP Tool Poisoning and Prompt Injection Detection"
# Tested on Python 3.9 (local) and Python 3.12 (Google Colab T4)
#
# Install: pip install -r requirements.txt
# Colab:   !pip install -r requirements.txt -q

# ── Core ML ───────────────────────────────────────────────────────────────────
numpy>=1.24,<3.0
pandas>=1.5,<3.0
scikit-learn>=1.3,<2.0
xgboost>=2.0,<3.0

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=2.7,<6.0
torch>=2.0               # CPU or GPU; Colab provides CUDA automatically

# ── Explainability ────────────────────────────────────────────────────────────
shap>=0.43,<1.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7,<4.0
tqdm>=4.60,<5.0

# ── Statistics ────────────────────────────────────────────────────────────────
scipy>=1.10,<2.0

# ── API server (serve/api.py) — optional for deployment only ─────────────────
fastapi>=0.100,<1.0
uvicorn>=0.22,<1.0
pydantic>=2.0,<3.0

# ── Development / notebook ───────────────────────────────────────────────────
jupyter>=1.0             # optional — for running notebooks locally
ipywidgets>=8.0          # optional — for Colab progress bars

# ── Verified version set (local macOS, 2026-06-01) ───────────────────────────
# numpy==2.0.2
# pandas==2.3.3
# scikit-learn==1.6.1
# xgboost==2.1.4
# sentence-transformers==5.1.2
# torch==2.8.0
# shap==0.49.1
# matplotlib==3.9.4
# tqdm==4.67.3
# scipy==1.13.1
